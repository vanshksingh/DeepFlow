# Atlas workspace

An intentionally imperfect multi-repository fixture for exercising DeepFlow. It contains a gateway, ingestion worker, shared data package, intentionally unused modules, a dynamic handler, and a failure path.
