// Intentional unresolved import: validates analyzer diagnostics for incomplete AI changes.
import { experimentalVectorStore } from '@atlas/does-not-exist';
export function connectBrokenStore() { return experimentalVectorStore(); }
